# POPS Application Dev Log

## Timestamp

- Date: 2026-06-25
- Documented start time: 2026-06-25 23:00:00 -05:00 approximate
- Handoff/update time: 2026-06-25 23:38:31 -05:00
- Workspace timezone: America/Chicago

## Canonical Repository

- Correct repository: `Spruked/POPS_Application`
- Local clone path: `C:\dev\Desktop\proof_of_presence_tauri\POPS_Application`
- Branch at clone: `main`
- Git state after clone/build checks: clean except this dev log and extraction artifacts created after the checks

## Work Performed

1. Compared two supplied POPS V1 Session One starter-pack drafts.
2. Identified the second draft as the architecture-aligned source because it preserves the existing app SQLite authority and rejects a duplicate standalone database.
3. Initially worked in the wrong local folder:
   - Wrong folder: `C:\dev\Desktop\proof_of_presence_tauri`
   - Mistake: started implementing Session One scaffolding there before the correct repo was identified.
   - Correction: stopped work in that folder and cloned the canonical repo into the workspace.
4. Cloned the correct repo with:
   - `gh repo clone Spruked/POPS_Application .\POPS_Application`
5. Confirmed the canonical repo already contains:
   - Tauri desktop app
   - React/Vite frontend
   - Rust/SQLite backend
   - existing `evidence_chain`
   - existing `audit_ledger`
   - preserved evidence originals workflow
   - TPC substrate under `Triple_Predicate_Cubed`
   - existing `Profile`, `VisitationCalendar`, `EvidenceVault`, `Incidents`, `PlayersDossier`, and other POPS pages
6. Confirmed Gradio is external in WSL projects and should remain a capability layer, not copied into this repo.
7. Confirmed there is no checked-in native iOS project yet:
   - no `ios/`
   - no `.xcodeproj`
   - no `.xcworkspace`
   - current app is Tauri 1.5 desktop
8. Installed dependencies in the canonical repo:
   - `npm install`
   - result: completed, 77 packages installed/audited
   - npm reported 2 vulnerabilities: 1 moderate, 1 high
9. Built the frontend:
   - command: `npm run build`
   - result: passed
   - output:
     - `dist/index.html`
     - `dist/assets/index-Lk52Kc_L.css`
     - `dist/assets/index-aS_Vkt8L.js`
10. Checked Rust/Tauri:
    - command: `cargo check`
    - first run failed because `src-tauri/tauri.conf.json` points at `../dist` and `dist` did not exist yet
    - after `npm run build`, `cargo check` passed

## Verification Evidence

Frontend build:

```text
dist/index.html                   0.48 kB | gzip: 0.31 kB
dist/assets/index-Lk52Kc_L.css   26.85 kB | gzip: 5.91 kB
dist/assets/index-aS_Vkt8L.js   316.19 kB | gzip: 86.53 kB
built in 9.63s
```

Rust/Tauri check:

```text
Checking proof-of-presence v1.0.0
Finished dev profile [unoptimized + debuginfo] target(s) in 10.13s
```

## VS Code / Codex Usage Note

- This session was run from VS Code/Codex against local files.
- The workflow was materially faster than the prior flow because repo inspection, build checks, clone correction, and command verification were done directly in the local workspace.
- The user noted the session had already consumed about 75 percent of the 5-hour usage limit by this point.
- Keep future turns repo-first and command-first to preserve time.

## Current iOS Status

- No iPhone app has been generated yet.
- Current validated artifact is the built web frontend in `dist/`.
- A true iOS app requires one of these next architecture choices:
  - initialize a mobile wrapper around the built frontend, or
  - migrate/build through a mobile-capable Tauri path, or
  - create an Xcode/iOS shell on macOS and point it at the local app bundle.
- Do not treat the desktop Tauri app as an `.ipa` until a native iOS wrapper exists and is built.

## Next Work

1. Package the current validated `dist/` output for extraction.
2. Decide the iPhone app route.
3. If staying local-first and deterministic, keep SQLite authority in Rust/Tauri and expose only approved local command surfaces to mobile.
4. Keep WSL Gradio as external tools inspected by live config; do not copy Gradio projects into the Tauri app.
